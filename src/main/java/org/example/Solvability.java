package org.example;

public class Solvability {
   private int N = 3;

    public int getNbInverse(int[] board) {
        // int[] board : single array representation of the game board
        int inverseCount = 0;
        int valueToCompare = -1;
        for (int i = 0; i < board.length; i++) {

            for (int j = i + 1; j < board.length; j++) {

            }
        }
        return(-1);
    }
}
